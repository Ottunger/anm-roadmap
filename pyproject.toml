[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ANM Roadmap"
authors = [{name = "Grégoire Mathonet", email = "gregoire@anotemusic.com"}]
dynamic = ["version", "description"]
